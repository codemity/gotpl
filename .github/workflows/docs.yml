name: Check

on:
  pull_request:
    branches:
      - main
  push:
    tags:
      - 'v*'

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Check
        run: make prep docs diff
